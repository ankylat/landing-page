import React from "react";

const AiModel = () => {
  return (
    <div>
      <p>every day, a maximum of 192 pieces of writing will happen</p>
      <p>
        each one of them will be fed into a custom ai model, that will be
        trained with the feedback that we give to it
      </p>
      <p>learning about ourselves</p>
      <p>bringing it into life</p>
    </div>
  );
};

export default AiModel;
