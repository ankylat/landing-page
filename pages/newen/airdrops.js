import React from "react";

const Tokenomics = () => {
  return (
    <div>
      <p>
        if you write 8 minutes every day, on this first cycle, you will be able
        to claim around 7k $newen per session.
      </p>
    </div>
  );
};

export default Tokenomics;
